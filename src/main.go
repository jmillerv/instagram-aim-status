package main

func main() {
	// load the json of artists
	// choose a random artist
	// get their track listings from api
	// select the most popular track
	// get the lyrics class from shazam
	// select the first 60 characters of the string
	// output the results
}